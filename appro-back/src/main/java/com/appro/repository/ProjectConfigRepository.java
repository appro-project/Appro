package com.appro.repository;

import com.appro.entity.ProjectConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectConfigRepository extends JpaRepository<ProjectConfig, Integer> {
}
